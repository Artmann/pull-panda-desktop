import { ReactElement } from 'react'

import { PullRequest } from '@/types/pullRequest'

export function CommitsView({
  pullRequest: _pullRequest
}: {
  pullRequest: PullRequest
}): ReactElement {
  return <div>CommitsView</div>
}
