import { ReactElement } from 'react'

import { PullRequest } from '@/types/pull-request'

export function FilesView({
  pullRequest: _pullRequest
}: {
  pullRequest: PullRequest
}): ReactElement {
  return <div>FilesView</div>
}
