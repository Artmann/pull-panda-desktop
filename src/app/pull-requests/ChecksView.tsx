import { ReactElement } from 'react'

import { PullRequest } from '@/types/pullRequest'

export function ChecksView({
  pullRequest: _pullRequest
}: {
  pullRequest: PullRequest
}): ReactElement {
  return <div>ChecksView</div>
}
